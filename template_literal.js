function doubleMessage(number) {
  return `${number}を2倍すると${(2 * number)}になります`;
}

console.log(doubleMessage(10));

function fullName(firstName, lastName) {
  return `${firstName} ${lastName}`;
}

console.log(fullName('yoshio', 'kinoshita'));